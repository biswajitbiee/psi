#include "classlib/GraphNode.h"
#include "ConstraintImp.h"
#include "FieldItemImp.h"

namespace pss {
  /*template<typename T>
  void GraphNode<T>::set_constraint(const Constraint& c)
  {
    FieldItemImp* fi = static_cast<FieldItemImp*>(this->m_field.impl());
    fi->set_constraint(ConstraintImp(c));
  }*/
}
